# Local env file written by the deploy workflow at runtime. Holds
# GitHub Secrets injected via docker-compose. Never commit.
.env

# Production env templates (committed as .example, real values stay local)
.env.production
backend/.env.production

# OS
.DS_Store
Thumbs.db

# Internal dev folders — not part of the shipped product
.design/
.guide/
